#include <stdio.h>

struct celula {
    int valor;
    CELULA prox;
};

typedef struct celula CELULA;

int* converte(int v[], int x) {

    CELULA lst = malloc(8);

    for (int i = 0; i < x; ++i) {

    }
}

int main() {

}